# ────────────────────────────────────────────────────────────────────────────
# Noor Prayer Times — Environment Variables
# ────────────────────────────────────────────────────────────────────────────
#
# Copy this file to `.env.local` for local development with `vercel dev`,
# AND set the same variables in:
#   Vercel → Project → Settings → Environment Variables
#   (scope: Production + Preview + Development)
# ────────────────────────────────────────────────────────────────────────────

# 1) Supabase project URL
#    ⚠️  Must start with "https://" and end with ".supabase.co"
#    ⚠️  NOT a "sb_publishable_…" key, NOT the JWT.
SUPABASE_URL=https://usapwzlbjjoxzlafmmrd.supabase.co

# 2) Supabase service-role JWT
#    Supabase Dashboard → Project Settings → API → service_role secret
#    Starts with "eyJ" — keep this secret, never expose to the client.
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InVzYXB3emxiampveHpsYWZtbXJkIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc3NjczMzQzMiwiZXhwIjoyMDkyMzA5NDMyfQ.0yhanvIYEMRlnti4oxRgPr-0YQ8ZZrUZR_QKWjMJmes

# 3) Admin password — used to unlock /admin and to sign the session JWT.
#    Pick a strong, unique value.
ADMIN_PASSWORD=ahmed1@2h